/**
 * Rule: soroban-serialization-cost (#774)
 * Wraps the serialization analyzer into a named rule.
 */
export { analyzeSerializationCosts, SerializationFinding, SerializationAnalysisResult } from '../analyzer/serialization-analyzer';
